---
id: msg-warning-219716
name: Warning Message
category: message
subcategory: warning
source_line: 219716
---


    );
  }
}
function dio() {
  return Dt().shiftEnterKeyBindingInstalled === !0;
}
function pio() {
  return Dt().hasUsedBackslashReturn === !0;
}
function fio() {
  if (!Dt().hasUsedBackslashReturn)
    yn((t) => ({ ...t, hasUsedBackslashReturn: !0 }));
}
async function nlp(e, t, n) {
  if (
    uoe.platform() === "darwin" &&
    process.env.__CFBundleIdentifier === "com.googlecode.iterm2" &&
    (De.terminal === "iTerm.app" ||
      De.terminal === "tmux" ||
      De.terminal === "screen" ||
      De.terminal === null)
  ) {
    let s = 
